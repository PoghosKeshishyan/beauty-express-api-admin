<h2><%= title %></h2>

<a href="/<%= title %>/create">Create New <%= title %></a>

<table>
    <tr>
        <th>N</th>
        <th>title</th>
        <th>url</th>
        <th>actions</th>
    </tr>

    <% navbars.forEach((elem, index) => { %>
        <tr>
            <td><%= index + 1 %></td>
            <td><%= elem.title %></td>
            <td><%= elem.url %></td>
            <td>
                <a href="/<%= title %>/edit/<%= elem.id %>">Edit</a>
                <a href="/<%= title %>/remove/<%= elem.id %>">Remove</a>
            </td>
        </tr>
    <% }) %>
</table>