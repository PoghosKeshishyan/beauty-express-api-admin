<h2><%= title %></h2>

<a href="/navbars/create">Create New <%= title %></a>

<table>
    <tr>
        <th>N</th>
        <th>title</th>
        <th>url</th>
        <th>actions</th>
    </tr>

    <% navbars.forEach((elem, index) => { %>
        <tr>
            <td><%= index + 1 %></td>
            <td><%= elem.title %></td>
            <td><%= elem.url %></td>
            <td>
                <a href="/navbars/edit/<%= elem.id %>">Edit</a>
                <a href="/navbars/remove/<%= elem.id %>">Remove</a>
            </td>
        </tr>
    <% }) %>
</table>